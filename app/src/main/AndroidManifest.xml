<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mitsuki.bottominput">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BottomInput">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".AdjustResizeActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".AdjustPanActivity"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".doublelist.DoubleListActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".fulldialog.FullScreenDialogActivity" />

        <activity android:name=".custom.BottomInputActivity" />

    </application>

</manifest>